#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <Windows.h>

// windows program

#define sex sex

// Haskell simulation
__attribute__((__pure__))char input [1ull<<28]={88};

// Interval
struct $$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$ {
    void (*q)(void);
    int32_t p;
};

void sex(void) { printf("\tㅇㅅㅇ\t\tHello world\t\tㅇㅅㅇ\n"); }

int main(void) {

    struct $$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$ * Windows = (struct $$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$*)malloc(sizeof(struct $$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$));
    Windows -> p = 88;
    Windows -> q = sex;

    SetConsoleOutputCP(CP_UTF8);

    while(0 <-- Windows -> p) {
        Windows -> q();
    }
    
    malloc(1ull<<666);
    printf("%*s %*s ㅇㅅㅇ %*s %*s");

    while (1) {
        __builtin_trap();
    }

    return __builtin_expect(69,_snprintf_c_l(NULL, 0, NULL, NULL, wcstombs_s(NULL, NULL, 0, NULL, 0)));

}
